import * as AlertDialog from "@radix-ui/react-alert-dialog";
import { AlertTriangle } from "lucide-react";

export function ConfirmActionDialog({ trigger, title, description, confirmation, onConfirmationChange, expected, busy, error, onConfirm }: { trigger: React.ReactNode; title: string; description: string; confirmation: string; onConfirmationChange: (value: string) => void; expected: string; busy: boolean; error?: string; onConfirm: () => void }) {
  return <AlertDialog.Root><AlertDialog.Trigger asChild>{trigger}</AlertDialog.Trigger><AlertDialog.Portal><AlertDialog.Overlay className="dialog-overlay" /><AlertDialog.Content className="dialog-content"><div className="dialog-warning"><AlertTriangle aria-hidden /></div><AlertDialog.Title>{title}</AlertDialog.Title><AlertDialog.Description>{description}</AlertDialog.Description><label htmlFor="archive-confirmation">Digite <strong>{expected}</strong> para confirmar<input id="archive-confirmation" value={confirmation} onChange={(event) => onConfirmationChange(event.target.value)} autoComplete="off" /></label>{error && <div className="form-error" role="alert">{error}</div>}<div className="dialog-actions"><AlertDialog.Cancel className="secondary-button">Cancelar</AlertDialog.Cancel><button className="danger-button" disabled={busy || confirmation !== expected} onClick={onConfirm}>{busy ? "Arquivando..." : "Arquivar cliente"}</button></div></AlertDialog.Content></AlertDialog.Portal></AlertDialog.Root>;
}
